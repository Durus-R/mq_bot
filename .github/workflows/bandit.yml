name: Bandit Vulnerabilitie scan

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
     - run: apt install python3-pip
     - run: pip3 install bandit
     - run: bandit -r ${{ github.workspace }}
     
     
  



